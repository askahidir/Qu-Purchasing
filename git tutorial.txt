Qu Purchasing
https://github.com/askahidir/Qu-Purchasing.git
Oracle E-Business Suite Enterprise Command Centers
 - March 2020 Update


Git

manage files, track history, eazy for team work

Most popular Version control System

help manage our project's files

(1) history
	Git keep tracks of every changes

(2) Collaboration(merging)


(3) feature branches

--Git key terms
-- Project =Repository(repo)
-- working directory
-- Staging = Control what gets committed
-- commit

(git status, git add -A, git git commit -m 'mmmmmmmm'

---git checkout -- .
 git stores the data localy in hidden files on hard drive.



Push & Pull
Github is server host your repository

--Install git
--find/open command -line
git-scm.com download for window
 
--git bash this window command line

git config --global user.name "Asia"
asiakhidir@MITSELAA1077306 MINGW64 ~ (BARE:master)
git config --global user.email "asiakhidir@qu.edu.qa"
asiakhidir@MITSELAA1077306 MINGW64 ~ (BARE:master)

 *create folder in your computer name i t like (Projects, sites,...)

 **return to git and see where current command line is pointing

pwd
**change direcory

cd **(type location/ or drage your folder)

ls **(list all files) 

pwd **(see last direcory)

mkdir "hellow-world" **(how to create new direcotory)


git init

**  ( tracking or create new repository in this git will track hellow-world folder)

touch "index.html" **  (create file under thi direcory)

git status  **(will show what change was done) 

git add index.html **(add this file to staging area) 

git add .
( ** add all files) 

git status **(show last changes)
git commit -m 'my first commit.' 
**( comment message in the history)

**(Make any change in your file and even you can delete)
git checkout -- .
**(restore the file back to where we maid our comment

Installed git
Open the command-line
Navigate to a directory in the command-line
Create a new repo with "git init"
Made a commit
---Clone copy and existing repository from a server to our computer

Clone

git clone https://github.com/LearnWebCode/welcome-to-git.git



















